<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="12dp">

    <TextView
        android:id="@+id/tvCategoryName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Category Name"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/btnSettings"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintHorizontal_bias="0" />

    <TextView
        android:id="@+id/tvCategoryMeta"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="created by X — date"
        android:textSize="12sp"
        app:layout_constraintStart_toStartOf="@id/tvCategoryName"
        app:layout_constraintTop_toBottomOf="@id/tvCategoryName"
        app:layout_constraintEnd_toEndOf="@id/tvCategoryName"
        android:layout_marginTop="6dp"/>

    <TextView
        android:id="@+id/seller"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="created by X — date"
        android:textSize="12sp"
        app:layout_constraintStart_toStartOf="@id/tvCategoryMeta"
        app:layout_constraintTop_toBottomOf="@id/tvCategoryMeta"
        app:layout_constraintEnd_toEndOf="@id/tvCategoryMeta"
        android:layout_marginTop="6dp"/>

    <ImageButton
        android:id="@+id/btnSettings"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Category settings"
        android:scaleType="centerInside"
        android:src="@android:drawable/ic_menu_more"
        app:tint="?android:attr/textColorPrimary"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
